@model Sistema_Financiero.Models.UsuarioModelo

@{
    ViewData["Title"] = "Editar Usuario";
}

<style>
    /* mismo estilo que Crear */
    .form-container {
        background: #ffffff;
        padding: 30px;
        border-radius: 12px;
        box-shadow: 0 4px 15px rgba(0,0,0,0.04);
        max-width: 500px;
        margin-top: 20px;
    }

    .form-group {
        margin-bottom: 18px;
    }

        .form-group label {
            display: block;
            margin-bottom: 6px;
            color: #475569;
            font-size: 0.85rem;
            font-weight: 500;
        }

    .form-control {
        width: 100%;
        padding: 10px 14px;
        border: 1px solid #cbd5e1;
        border-radius: 8px;
        font-size: 0.95rem;
        box-sizing: border-box;
        outline: none;
    }

        .form-control:focus {
            border-color: #2b6cb0;
            box-shadow: 0 0 0 4px rgba(43,108,176,0.1);
        }

    .btn-guardar {
        background: #1e40af;
        color: white;
        border: none;
        padding: 10px 24px;
        border-radius: 8px;
        font-size: 0.95rem;
        font-weight: 600;
        cursor: pointer;
    }

    .btn-cancelar {
        background: #f1f5f9;
        color: #475569;
        border: none;
        padding: 10px 24px;
        border-radius: 8px;
        font-size: 0.95rem;
        font-weight: 600;
        cursor: pointer;
        text-decoration: none;
        display: inline-block;
        margin-left: 10px;
    }
</style>

<h2 style="font-family:'Segoe UI',sans-serif;color:#2d3748;font-weight:600;margin-bottom:5px;">
    Editar Usuario
</h2>
<p style="color:#718096;margin-bottom:20px;">Modificá los datos del usuario seleccionado.</p>

@if (ViewBag.Error != null)
{
    <div class="alert-danger" style="margin-bottom:15px;">@ViewBag.Error</div>
}

<div class="form-container">
    <form action="@Url.Action("Editar", "Usuarios")" method="POST">

        <input type="hidden" name="CodigoUsuario" value="@Model.CodigoUsuario" />

        <div class="form-group">
            <label>Código Empleado</label>
            <input type="number" name="CodigoEmpleado" class="form-control" value="@Model.CodigoEmpleado" required />
        </div>

        <div class="form-group">
            <label>Nombre de Usuario</label>
            <input type="text" name="Nombre" class="form-control" value="@Model.Nombre" required />
        </div>

        <div class="form-group">
            <label>Rol</label>
            <select name="CodigoRol" class="form-control" required>
                <option value="">-- Seleccione un Rol --</option>
                @if (ViewBag.Roles != null)
                {
                    @foreach (var rol in (List<Sistema_Financiero.Models.RolModelo>)ViewBag.Roles)
                    {
                        <!option value="@rol.CodigoRol" @(rol.CodigoRol == Model.CodigoRol ? "selected" : "")>
                            @rol.Nombre
                        </!option>
                    }
                }
            </select>
        </div>

        <div class="form-group">
            <label>Estado</label>
            <select name="Estado" class="form-control">
                @if (Model.Estado)
                {
                    <option value="true" selected>Activo</option>
                    <option value="false">Inactivo</option>
                }
                else
                {
                    <option value="true">Activo</option>
                    <option value="false" selected>Inactivo</option>
                }
            </select>
        </div>
            <div class="form-group">
    <label>Nueva Contraseña (Dejar vacío para no cambiar)</label>
    <input type="password" name="Clave" class="form-control" placeholder="Escribir Nueva contraseña" />
</div>


        <button type="submit" class="btn-guardar">Guardar Cambios</button>
        <a href="@Url.Action("Index", "Usuarios")" class="btn-cancelar">Cancelar</a>
    </form>
</div>